package com.telenav.osv.common.toolbar;

/**
 * Interface defining the action for a toolbar menu item.
 */
public interface MenuAction {

    /**
     * Method defining the action for a toolbar menu item.
     */
    void onClick();
}
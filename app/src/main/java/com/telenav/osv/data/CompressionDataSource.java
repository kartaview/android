package com.telenav.osv.data;

/**
 * Base interface for compression data source. This is made to comply with the logic in the local data model where video and photo are considered as a compression concrete
 * implementation of a base.
 * @author horatiuf
 */
public interface CompressionDataSource {
    //there is default common logic to be inherited, see description of the interface.
}
